# ──────────────────────────────────────────────────────────────
# CashU — Python dependencies
# Install: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# Web framework
Flask==3.1.0
flask-restx==1.3.0
flask-cors==5.0.0
Werkzeug==3.1.3

# Database
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
SQLAlchemy==2.0.36
alembic==1.14.0
PyMySQL==1.1.1                  # MySQL driver
cryptography==44.0.0            # PyMySQL sha2 auth + AES-256-GCM field encryption

# Auth / security
Flask-JWT-Extended==4.7.1
PyJWT==2.10.1
pyotp==2.9.0                    # TOTP scaffolding for 2FA
Flask-Session==0.8.0

# Environment
python-dotenv==1.0.1

# Email
Flask-Mail==0.10.0

# Background jobs — scheduler.py (pre-debit notices, retries, recon, polling)
APScheduler==3.11.0

# HTTP — Cashfree REST calls (PG, Payouts, Verification Suite)
requests==2.32.3

# Penny-drop name matching (PRD FR-005: Jaro-Winkler / Levenshtein)
rapidfuzz==3.14.6

# PDF receipts (PRD FR-008 digitally signed receipt, US-014 tax receipts)
reportlab==4.2.5

# Testing
pytest==8.3.4
pytest-flask==1.3.0

# Production server
gunicorn==23.0.0
